type TableHeadSelectProps = {
  columnIndex: number;
  selectedColumns: any;
  onChange: any;
};

const TableHeadSelect = ({
  columnIndex,
  selectedColumns,
  onChange,
}: TableHeadSelectProps) => {
  return <div>TableHeadSelect</div>;
};

export default TableHeadSelect;
